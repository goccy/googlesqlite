spec: docs/specs/spanner/functions/compression/zstd_decompress_to_bytes.md
dialect: spanner
# Candidate testdata sourced from googlesql compliance fixtures.
# Review each case before committing as authoritative.
cases:
  - desc: zstd_decompress_to_bytes_null
    sql: |-
      select zstd_decompress_to_bytes(null)
    expected:
      rows:
        - [null]
  - desc: zstd_decompress_to_bytes_optional_arg_null
    sql: |-
      select zstd_decompress_to_bytes(b"(\xb5/\xfd \x05)\x00\x00bytes", size_limit => NULL)
    expected:
      rows:
        - [null]
