spec: docs/specs/bigquery/functions/kll/kll_quantiles_merge_point_int64.md
dialect: bigquery
cases:
  - desc: kll_quantiles_merge_point_int64 runs end-to-end
    sql: SELECT KLL_QUANTILES.MERGE_POINT_INT64(s, 0.5) IS NOT NULL FROM (SELECT KLL_QUANTILES.INIT_INT64(x) AS s FROM UNNEST([1,2,3]) AS x)
    expected:
      rows:
        - [true]
