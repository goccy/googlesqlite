spec: docs/specs/googlesql/functions/geography/st_astext.md
dialect: googlesql
cases:
  - desc: format a point as WKT
    sql: SELECT ST_ASTEXT(ST_GEOGPOINT(1, 2))
    expected:
      rows:
        - ['POINT (1 2)']
