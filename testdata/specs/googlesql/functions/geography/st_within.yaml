spec: docs/specs/googlesql/functions/geography/st_within.md
dialect: googlesql
cases:
  - desc: Point within polygon
    sql: |-
      SELECT ST_WITHIN(ST_GEOGFROMTEXT('POINT(0.5 0.5)'), ST_GEOGFROMTEXT('POLYGON((0 0, 1 0, 1 1, 0 1, 0 0))'))
    expected:
      rows:
        - [true]
