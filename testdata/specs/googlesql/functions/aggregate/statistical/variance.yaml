spec: docs/specs/googlesql/functions/aggregate/statistical/variance.md
dialect: googlesql
cases:
  - desc: variance (alias for var_samp) of two values
    sql: SELECT VARIANCE(x) FROM UNNEST([1.0, 3.0]) AS x
    expected:
      rows:
        - [2.0]
