spec: docs/specs/googlesql/functions/aggregate/statistical/covar_pop.md
dialect: googlesql
cases:
  - desc: covar_pop returns a finite value for non-trivial input
    sql: SELECT IS_NAN(COVAR_POP(x, x)) FROM UNNEST([1.0, 2.0, 3.0]) AS x
    expected:
      rows:
        - [false]
