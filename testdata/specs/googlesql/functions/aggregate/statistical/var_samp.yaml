spec: docs/specs/googlesql/functions/aggregate/statistical/var_samp.md
dialect: googlesql
cases:
  - desc: var_samp of two values
    sql: SELECT VAR_SAMP(x) FROM UNNEST([1.0, 3.0]) AS x
    expected:
      rows:
        - [2.0]
