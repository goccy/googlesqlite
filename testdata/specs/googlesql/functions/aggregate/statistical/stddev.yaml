spec: docs/specs/googlesql/functions/aggregate/statistical/stddev.md
dialect: googlesql
cases:
  - desc: stddev (alias for stddev_samp) of two values
    sql: SELECT ROUND(STDDEV(x), 4) FROM UNNEST([1.0, 3.0]) AS x
    expected:
      rows:
        - [1.4142]
