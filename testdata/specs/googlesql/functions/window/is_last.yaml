spec: docs/specs/googlesql/functions/window/is_last.md
dialect: googlesql
cases:
  - desc: IS_LAST returns BOOL via numbering rewrite
    sql: SELECT TYPEOF(IS_LAST(2) OVER (ORDER BY x)) FROM UNNEST([1,2,3]) AS x LIMIT 1
    expected:
      rows:
        - ['BOOL']
