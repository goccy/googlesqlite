spec: docs/specs/googlesql/functions/window/percent_rank.md
dialect: googlesql
cases:
  - desc: percent rank produces a finite numeric value
    sql: SELECT IS_NAN(PERCENT_RANK() OVER (ORDER BY x)) FROM UNNEST([1,2,3]) AS x LIMIT 1
    expected:
      rows:
        - [false]
