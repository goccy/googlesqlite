spec: docs/specs/googlesql/functions/window/dense_rank.md
dialect: googlesql
cases:
  - desc: dense_rank with ties
    sql: |
      SELECT x, DENSE_RANK() OVER (ORDER BY x) AS r
      FROM UNNEST([10, 20, 20, 30]) AS x
      ORDER BY x
    expected:
      rows:
        - [10, 1]
        - [20, 2]
        - [20, 2]
        - [30, 3]
