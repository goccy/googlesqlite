spec: docs/specs/googlesql/functions/window/cume_dist.md
dialect: googlesql
cases:
  - desc: cumulative distribution over a 3-row partition
    sql: SELECT IS_NAN(CUME_DIST() OVER (ORDER BY x)) FROM UNNEST([1,2,3]) AS x LIMIT 1
    expected:
      rows:
        - [false]
