spec: docs/specs/googlesql/syntax/ddl/create_function.md
dialect: googlesql
cases:
  - desc: CREATE FUNCTION registers a callable UDF
    setup:
      - CREATE FUNCTION fn_ds.add_one(x INT64) AS (x + 1)
    sql: SELECT fn_ds.add_one(41) AS r
    expected:
      rows:
        - [42]
